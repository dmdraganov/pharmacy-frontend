import { memo } from 'react';
import { ProfileDetails } from '@/widgets/profile/ProfileDetails';

const AccountProfilePage = memo(() => {
  return <ProfileDetails />;
});

export default AccountProfilePage;
