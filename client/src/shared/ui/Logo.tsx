import { Link } from 'react-router-dom';

const Logo = () => {
  return (
    <Link to='/' className='shrink-0 text-xl font-bold text-text-default sm:text-2xl'>
      DivMedica
    </Link>
  );
};

export default Logo;
